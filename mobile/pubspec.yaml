name: mobile
description: Un projet Flutter

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^4.0.0
  latlong2: ^0.8.1
  geolocator: ^14.0.0
  cupertino_icons: ^1.0.2
  fl_chart: ^0.66.2

flutter:
  uses-material-design: true
  assets:
    - assets/image/running.png
